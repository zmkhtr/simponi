<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sipemandu.sipemandu">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".UI.BlankContainer.BlankActivity"
            android:parentActivityName=".UI.BlankContainer.BlankActivity"
            android:resizeableActivity="false"/>
        <activity android:name=".UI.LoginActivity.LoginActivity"
            android:resizeableActivity="false"/>
        <activity android:name=".UI.SplashScreen.SplashScreenActivity"
            android:resizeableActivity="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.MainActivity.MainActivity"
            android:resizeableActivity="false"/>
    </application>

</manifest>